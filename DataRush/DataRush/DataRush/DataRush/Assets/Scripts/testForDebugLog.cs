using UnityEngine;

public class testForDebugLog: MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Test script is running.");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
